import { StyleSheet } from 'react-native'

export default StyleSheet.create({

    container: {
        padding: 10,
        marginTop: 10,
        borderWidth: 2,
        borderRadius: 8,
        borderColor: 'white',
        width: 300,
        backgroundColor: '#f6f6f6',
        color: '#FFFFFF',
    },
});